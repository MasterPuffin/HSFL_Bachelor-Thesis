{% extends "base.twig" %}
{% block main %}
    <h1>News</h1>
    <hr class="mt-2 mb-4">
    <div class="row">
        {% for newsItem in news %}
            {{ include('components/newsGridItem.twig') }}
        {% endfor %}
    </div>
{% endblock %}